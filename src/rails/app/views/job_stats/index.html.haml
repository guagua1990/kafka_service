%h1 Attribution Job Log Stats

%table#job_stats
  %thead
    %tr
      %th Job Id
      %th Errors
      %th Actual Total Count
      %th Expected Total Count
  %tbody
    - @job_stats.each do |stat|
      %tr
        %td= stat.job_id
        %td= stat.count_error
        %td= stat.count_actual_total
        %td= stat.count_expected_total
